# Context association

Generated by `see.js` from a Notation3 source file.

Uses an external RDF/TriG input dataset whose named graph blocks associate a
data graph, a signature graph, and a metadata graph. The compiled SEE example
verifies that the graph contexts form a signed association around Bob's data.

## Compilation summary

- Example name: `context_association`
- Input facts emitted: 0
- Forward rules compiled: 2
- Backward predicate rules compiled: 0
- Fuses compiled: 0
- Predicate count: 11

## Built-ins used

- `log:includes`
- `log:nameOf`
- `log:outputString`

## Runtime model

The generated `examples/context_association.js` is a specialized JavaScript derivation program. For ordinary sources, `see.js` emits the source facts as `examples/input/context_association.trig`. For rules-only sources, generation can reuse an existing external evidence file named `examples/input/context_association.trig`. The runner reads that TriG evidence directly and performs a local fixpoint derivation; it does not parse the program source or call an external reasoner.

## Output model

Running `node examples/context_association.js` produces a SEE-style Markdown report with an **Entailment** section, an **Explanation** section, and a **Formal TriG Output** section containing the selected derived/query facts.
